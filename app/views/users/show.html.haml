- content_for :title do
  URRV
  \|
  = @user.username
/- if policy(@user).index?
= link_to 'Users', users_path
/- else
/  = link_to 'My Students', my_students_enrollments_path
\/
= @user.username
%p

= render 'users/user', user: @user